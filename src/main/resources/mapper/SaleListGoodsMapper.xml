<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.atguigu.jxc.dao.SaleListGoodsDao">

    <resultMap id="BaseResultMap" type="com.atguigu.jxc.entity.SaleListGoods">
        <result column="sale_list_goods_id" property="saleListGoodsId" jdbcType="INTEGER" />
        <result column="goods_id" property="goodsId" jdbcType="INTEGER" />
        <result column="goods_code" property="goodsCode" jdbcType="VARCHAR" />
        <result column="goods_name" property="goodsName" jdbcType="VARCHAR" />
        <result column="goods_model" property="goodsModel" jdbcType="VARCHAR" />
        <result column="goods_unit" property="goodsUnit" jdbcType="VARCHAR" />
        <result column="goods_num" property="goodsNum" jdbcType="INTEGER"/>
        <result column="price" property="price" jdbcType="FLOAT"/>
        <result column="total" property="total" jdbcType="FLOAT"/>
        <result column="sale_list_id" property="saleListId" jdbcType="INTEGER"/>
        <result column="goods_type_id" property="goodsTypeId" jdbcType="INTEGER"/>
    </resultMap>
    <select id="list" resultMap="BaseResultMap">
        select
        sale_list_goods_id,
        goods_id,
        goods_code,
        goods_name,
        goods_model,
        goods_unit,
        goods_num,
        price,
        total,
        sale_list_id,
        goods_type_id
        from
        t_sale_list_goods
        <where>
            <if test="saleListIds != null ">
                sale_list_id in
                <foreach collection="saleListIds" item="saleListId" separator="," open="(" close=")">
                    #{saleListId}
                </foreach>
            </if>
        </where>
    </select>

    <select id="getSaleDataByDay" resultType="java.lang.Integer">
        SELECT
            COUNT(*)
        FROM
        t_sale_list_goods
        <where>
            <if test="saleListIds != null ">
                sale_list_id in
                <foreach collection="saleListIds" item="saleListId" separator="," open="(" close=")">
                    #{saleListId}
                </foreach>
            </if>
        </where>
    </select>











</mapper>
